<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
          http://www.springframework.org/schema/beans/spring-beans-4.2.xsd">

    <bean id="ActiveMqConnectionFactory" class="org.apache.activemq.ActiveMQConnectionFactory">
        <!-- Add prefetch limit in brokerURL to limit the maximum number of messages that can be dispatched to the consumer at once.
		tcp://localhost:61616?jms.prefetchPolicy.queuePrefetch=10 -->
		<property name="brokerURL" value="failover:(tcp://activemq-0.activemq.activemq.svc.cluster.local:61616,tcp://activemq-1.activemq.activemq.svc.cluster.local:61616)?jms.prefetchPolicy.queuePrefetch=10"/>
        <property name="userName" value="admin"/>
        <property name="password" value="${temn.tti.jms.security.password}"/>
    </bean>

    <bean id="jmsConnectionFactoryCache" class="org.springframework.jms.connection.CachingConnectionFactory">
        <constructor-arg ref="ActiveMqConnectionFactory"/>
        <property name="sessionCacheSize" value="100"/>
    </bean>

    <bean id="jmsTransactionManager" class="org.springframework.jms.connection.JmsTransactionManager">
        <property name="connectionFactory" ref="jmsConnectionFactoryCache"/>
    </bean>

    <bean id="jmsConnectionFactoryInOut" class="org.apache.activemq.ActiveMQConnectionFactory">
        <property name="brokerURL" value="failover:(tcp://activemq-0.activemq.activemq.svc.cluster.local:61616,tcp://activemq-1.activemq.activemq.svc.cluster.local:61616)?jms.prefetchPolicy.queuePrefetch=10"/>
        <property name="userName" value="admin"/>
        <property name="password" value="${temn.tti.jms.security.password}"/>
    </bean>

    <bean id="jms" class="org.apache.camel.component.jms.JmsComponent">
        <property name="connectionFactory" ref="jmsConnectionFactoryCache"/>
        <property name="transactionManager" ref="jmsTransactionManager"/>
        <property name="transacted" value="true"/>
        <property name="concurrentConsumers" value="${concurrentConsumers}"/>
        <property name="maxConcurrentConsumers" value="${maxConcurrentConsumers}"/>
        <property name="cacheLevelName" value="CACHE_CONSUMER"/>
    </bean>
</beans>